export class city {
    id: number
    postCode: string 
    name: string
    cuit: number
    code: number

    constructor(id: number, postCode: string, name: string, cuit: number, code: number){
        this.id = id;
        this.postCode = postCode;
        this.name = name;
        this. cuit = cuit;
        this.code = code;
    }
}